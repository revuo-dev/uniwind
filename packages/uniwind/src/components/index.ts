import * as RN from 'react-native'
import { createUniWindComponent } from './createUniwindComponent'

export const View = createUniWindComponent(RN.View)
export const Text = createUniWindComponent(RN.Text)
